<%# <h1>Sessions#new</h1>
<p>Find me in app/views/sessions/new.html.erb</p> %>

<!--<h1>Login</h1>

<%= form_with(url: login_path, method: :post) do |form| %>
  <%= form.label :session, 'Email' %>
  <%= form.email_field :'session[email]' %>

  <%= form.label :session, 'Password' %>
  <%= form.password_field :'session[password]' %>

  <%= form.submit 'Login' %>
<% end %>-->



<!--<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Login</title>
    <%# <link rel="stylesheet" href="style.css"> %>
    <%= stylesheet_link_tag 'session/new' %>

  </head>
  <body>
    <h1>Welocome to Social-pulse</h1>
    <p>Be ready to run the world on your fingers</p>

    
    <%= form_with(url: login_path, method: :post) do |form| %>
      <%= form.label :session, 'Email' %>
      <%= form.email_field :'session[email]' %>

      <%= form.label :session, 'Password' %>
      <%= form.password_field :'session[password]' %>

      <%= form.submit 'Login' %>
    <% end %>
	<%# <script src="index.js"></script> %>
  </body>
</html>-->



<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Login</title>
    <%= stylesheet_link_tag 'session/new' %>
    <style>
      body {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        height: 100vh;
        margin: 0;
      }
      .content {
        text-align: center;
        margin-top: 20px;
      }
      form {
        margin-top: 50px;
      }
    </style>
  </head>
  <body>
    <div class="content">
      <h1>Welcome to Social-pulse</h1>
      <p>Be ready to run the world on your fingers</p>
    </div>
    
    <div class="form">
      <%= form_with(url: login_path, method: :post) do |form| %>
        <%= form.label :session, 'Email' %>
        <%= form.email_field :'session[email]' %>

        <%= form.label :session, 'Password' %>
        <%= form.password_field :'session[password]' %>

        <%= form.submit 'Login' %>
      <% end %>
    </div>

    <div class="signup">
     <p>or create a new account</p> <%= link_to 'Sign Up', new_user_path %>
    </div>
    <%# <script src="index.js"></script> %>
  </body>
</html>
